#!/usr/bin/env bash
set -euo pipefail
SCRIPT_DIR="$(cd -- "$(dirname -- "${BASH_SOURCE[0]}")" && pwd)"
PYTHON_BIN="${XCLAW_AGENT_PYTHON_BIN:-python3}"
PYTHONPATH="${SCRIPT_DIR}/..:${PYTHONPATH:-}" exec "$PYTHON_BIN" -m xclaw_agent.cli "$@"
