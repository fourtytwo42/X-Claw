export async function sendTelegramText(bot, chatId, text, runtime, opts) {
	const htmlText = markdownToTelegramHtmlChunks(text).join("\n");
	return await bot.api.sendMessage(chatId, htmlText, {
		parse_mode: "HTML",
	});
}
