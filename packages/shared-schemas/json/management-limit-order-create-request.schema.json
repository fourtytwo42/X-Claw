{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "type": "object",
  "additionalProperties": false,
  "required": ["agentId", "chainKey", "mode", "side", "tokenIn", "tokenOut", "amountIn", "limitPrice", "slippageBps"],
  "properties": {
    "agentId": { "type": "string", "minLength": 1 },
    "chainKey": { "type": "string", "minLength": 1 },
    "mode": { "type": "string", "enum": ["mock", "real"] },
    "side": { "type": "string", "enum": ["buy", "sell"] },
    "tokenIn": { "type": "string", "pattern": "^(0x[a-fA-F0-9]{40}|[1-9A-HJ-NP-Za-km-z]{32,44})$" },
    "tokenOut": { "type": "string", "pattern": "^(0x[a-fA-F0-9]{40}|[1-9A-HJ-NP-Za-km-z]{32,44})$" },
    "amountIn": { "type": "string", "pattern": "^[0-9]+(\\.[0-9]{1,18})?$" },
    "limitPrice": { "type": "string", "pattern": "^[0-9]+(\\.[0-9]{1,18})?$" },
    "slippageBps": { "type": "integer", "minimum": 1, "maximum": 10000 },
    "expiresAt": { "type": "string", "format": "date-time" }
  }
}
